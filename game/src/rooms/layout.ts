/** Default walkable gap (right / center). */
export const PASSAGE = 20;

/** Wider gaps for the left-office maze. */
export const PASSAGE_LEFT = 28;

/** Inner wall thickness (horizontal and vertical segments). */
export const SEG = 10;

/** Minimum inset from the inner perimeter edge (perimeter is 8px; first maze wall at 24+). */
export const INSET = 24;
